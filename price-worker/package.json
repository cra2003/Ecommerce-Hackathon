{
	"name": "price-worker",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"lint": "eslint .",
		"format": "prettier --write .",
		"lint:fix": "eslint . --fix",
		"test": "mocha test/**/*.spec.js --recursive",
		"test:coverage": "NODE_ENV=test c8 --reporter=text --reporter=html mocha --recursive --exit test/**/*.spec.js"
	},
	"dependencies": {
		"@microlabs/otel-cf-workers": "^1.0.0-rc.52",
		"@opentelemetry/api": "^1.9.0",
		"hono": "^4.10.6",
		"jose": "^6.1.2"
	}
}
