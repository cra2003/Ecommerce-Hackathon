name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # -------------------------------
  # 0) Detect Changed Workers
  # -------------------------------
  detect-changes:
    name: Detect Changed Workers
    runs-on: ubuntu-latest
    outputs:
      workers: ${{ steps.changes.outputs.workers }}
      workers-json: ${{ steps.changes.outputs.workers-json }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed workers
        id: changes
        run: |
          # Get list of changed files
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
            HEAD_SHA="${{ github.sha }}"
            CHANGED_FILES=$(git diff --name-only $BASE_SHA $HEAD_SHA)
          else
            git fetch origin main || true
            CHANGED_FILES=$(git diff --name-only origin/main...HEAD 2>/dev/null || git diff --name-only HEAD~1 HEAD)
          fi
          
          echo "Changed files:"
          echo "$CHANGED_FILES"
          
          # Define all workers
          WORKERS="auth-worker cart-worker cart-cron-worker order-worker payment-worker fullfilment-worker price-worker product-worker"
          
          # Detect which workers have changes
          CHANGED_WORKERS=""
          for worker in $WORKERS; do
            if echo "$CHANGED_FILES" | grep -q "^$worker/"; then
              CHANGED_WORKERS="$CHANGED_WORKERS $worker"
              echo "✅ $worker has changes"
            else
              echo "⏭️  $worker has no changes, skipping"
            fi
          done
          
          # Trim leading space
          CHANGED_WORKERS=$(echo "$CHANGED_WORKERS" | xargs)
          
          if [ -z "$CHANGED_WORKERS" ]; then
            echo "⚠️  No workers changed, nothing to deploy"
            echo "workers=" >> $GITHUB_OUTPUT
            echo "workers-json=[]" >> $GITHUB_OUTPUT
          else
            echo "Changed workers: $CHANGED_WORKERS"
            echo "workers=$CHANGED_WORKERS" >> $GITHUB_OUTPUT
            
            # Create JSON array for matrix
            JSON_ARRAY="["
            FIRST=true
            for worker in $CHANGED_WORKERS; do
              if [ "$FIRST" = true ]; then
                FIRST=false
              else
                JSON_ARRAY="$JSON_ARRAY,"
              fi
              JSON_ARRAY="$JSON_ARRAY\"$worker\""
            done
            JSON_ARRAY="$JSON_ARRAY]"
            echo "workers-json=$JSON_ARRAY" >> $GITHUB_OUTPUT
          fi

  # -------------------------------
  # 1) CI Checks (Lint + Format + Test + Knip)
  # -------------------------------
  ci:
    name: CI Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            **/node_modules
            ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

      - name: Run tests
        run: |
          WORKERS="auth-worker cart-worker cart-cron-worker order-worker payment-worker fullfilment-worker price-worker product-worker"
          HAS_TESTS=false
          
          for worker in $WORKERS; do
            if [ -f "$worker/package.json" ] && grep -q '"test"' "$worker/package.json"; then
              echo "🧪 Running tests for $worker..."
              cd "$worker"
              if npm test 2>&1; then
                echo "✅ $worker: tests passed"
                HAS_TESTS=true
              else
                echo "❌ $worker: tests failed"
                exit 1
              fi
              cd ..
            fi
          done
          
          if [ "$HAS_TESTS" = false ]; then
            echo "ℹ️  No tests found, skipping test step"
          fi

      - name: Run knip (non-blocking)
        continue-on-error: true
        run: |
          WORKERS="auth-worker cart-worker cart-cron-worker order-worker payment-worker fullfilment-worker price-worker product-worker"
          HAS_ERRORS=false
          
          for worker in $WORKERS; do
            if [ -f "$worker/package.json" ] && grep -q '"knip"' "$worker/package.json"; then
              echo "🔍 Running knip for $worker..."
              cd "$worker"
              if npm run knip 2>&1; then
                echo "✅ $worker: knip passed"
              else
                echo "⚠️  $worker: knip found issues (non-blocking)"
                HAS_ERRORS=true
              fi
              cd ..
            fi
          done
          
          if [ "$HAS_ERRORS" = true ]; then
            echo "⚠️  Some workers have knip issues (non-blocking)"
          fi

  # -------------------------------
  # 2) Preview Deploy for PRs
  # -------------------------------
  preview_deploy:
    name: Deploy Preview (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, ci]
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.workers != ''
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    continue-on-error: true
    outputs:
      deploy_status: ${{ steps.deploy_preview.outcome }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Preview Environment
        id: deploy_preview
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ${{ matrix.worker }}
          command: deploy --env preview

      - name: Slack Notify — Preview Deploy Failed
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"❌ *Preview deploy FAILED for ${{ matrix.worker }}* (PR #${{ github.event.pull_request.number }})\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

      - name: Slack Notify — Preview Deploy Success
        if: success()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"✅ *Preview deploy succeeded for ${{ matrix.worker }}* (PR #${{ github.event.pull_request.number }})\n🌐 https://${{ matrix.worker }}-preview.aadhi18082003.workers.dev\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

  # -------------------------------
  # 2.5) Preview Health Check
  # -------------------------------
  preview_health_check:
    name: Preview Health Check (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, preview_deploy]
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.workers != ''
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    continue-on-error: true
    outputs:
      status: ${{ steps.check.outcome }}
    steps:
      - name: Wait before preview health check
        run: sleep 10

      - name: Check preview health
        id: check
        run: |
          echo "Running preview health check for ${{ matrix.worker }}..."
          WORKER_NAME="${{ matrix.worker }}"
          # Handle different worker name formats
          if [ "$WORKER_NAME" = "order-worker" ]; then
            URL="https://orders-worker-preview.aadhi18082003.workers.dev/health"
          elif [ "$WORKER_NAME" = "product-worker" ]; then
            URL="https://products-worker-preview.aadhi18082003.workers.dev/health"
          else
            URL="https://${WORKER_NAME}-preview.aadhi18082003.workers.dev/health"
          fi
          curl -f "$URL" || exit 1

  # -------------------------------
  # 2.6) Automatic Preview Rollback
  # -------------------------------
  rollback_preview:
    name: Rollback Preview (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, preview_health_check]
    if: |
      github.event_name == 'pull_request' &&
      needs.detect-changes.outputs.workers != '' &&
      needs.preview_health_check.result != 'success'
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Rollback preview to previous version
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ${{ matrix.worker }}
          command: rollback --env preview

      - name: Slack Notify — Preview Auto Rollback
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"⚠️ *Preview health FAILED for ${{ matrix.worker }}* — auto rollback executed (PR #${{ github.event.pull_request.number }}).\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

  # -------------------------------
  # 3) Production Deploy
  # -------------------------------
  deploy_prod:
    name: Deploy to Production (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, ci]
    if: |
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      needs.detect-changes.outputs.workers != ''
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    continue-on-error: true
    outputs:
      deploy_status: ${{ steps.deploy_prod.outcome }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Production
        id: deploy_prod
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ${{ matrix.worker }}
          command: deploy

      - name: Slack Notify — Prod Deploy Failed
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"❌ *Production deploy FAILED for ${{ matrix.worker }}!*\nCommit: ${{ github.sha }} by ${{ github.actor }}\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

      - name: Slack Notify — Prod Deploy Success
        if: success()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            WORKER_NAME="${{ matrix.worker }}"
            if [ "$WORKER_NAME" = "order-worker" ]; then
              URL="https://orders-worker.aadhi18082003.workers.dev/health"
            elif [ "$WORKER_NAME" = "product-worker" ]; then
              URL="https://products-worker.aadhi18082003.workers.dev/health"
            else
              URL="https://${WORKER_NAME}.aadhi18082003.workers.dev/health"
            fi
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"✅ *Production deploy succeeded for ${{ matrix.worker }}!*\n🌐 $URL\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

  # -------------------------------
  # 3.5) Production Health Check
  # -------------------------------
  health_check_prod:
    name: Production Health Check (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, deploy_prod]
    if: |
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      needs.detect-changes.outputs.workers != ''
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    continue-on-error: true
    outputs:
      status: ${{ steps.check.outcome }}
    steps:
      - name: Wait before health check
        run: sleep 10

      - name: Check production health
        id: check
        run: |
          echo "Running production health check for ${{ matrix.worker }}..."
          WORKER_NAME="${{ matrix.worker }}"
          if [ "$WORKER_NAME" = "order-worker" ]; then
            URL="https://orders-worker.aadhi18082003.workers.dev/health"
          elif [ "$WORKER_NAME" = "product-worker" ]; then
            URL="https://products-worker.aadhi18082003.workers.dev/health"
          else
            URL="https://${WORKER_NAME}.aadhi18082003.workers.dev/health"
          fi
          curl -f "$URL" || exit 1

  # -------------------------------
  # 3.6) Automatic Production Rollback
  # -------------------------------
  rollback_prod:
    name: Rollback Production (${{ matrix.worker }})
    runs-on: ubuntu-latest
    needs: [detect-changes, health_check_prod]
    if: |
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      needs.detect-changes.outputs.workers != '' &&
      needs.health_check_prod.result != 'success'
    strategy:
      matrix:
        worker: ${{ fromJson(needs.detect-changes.outputs.workers-json) }}
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Rollback to previous version
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ${{ matrix.worker }}
          command: rollback

      - name: Slack Notify — PROD Auto Rollback
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\": \"🚨 *PRODUCTION HEALTH FAILED for ${{ matrix.worker }}* — Auto rollback executed.\n🔁 System restored to last working version.\"}" \
              $SLACK_WEBHOOK_URL || true
          fi

